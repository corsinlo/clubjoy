{{ 'chosen-by.css' | asset_url | stylesheet_tag }}

<div class="page-width">
  {% if section.settings.title != blank %}
    <h2 class="chosen-by-title">{{ section.settings.title }}</h2>
  {% endif %}

  {% if section.settings.subtitle != blank %}
    <p class="chosen-by-subtitle">{{ section.settings.subtitle }}</p>
  {% endif %}
<div class="chosen-by-section">


  <div class="chosen-by-logos-wrapper">
    <div class="chosen-by-logos-scroller js-chosen-by-scroller">
      <div class="chosen-by-logos-list-wrapper js-chosen-by-list-wrapper">
        <div class="chosen-by-logos-list js-chosen-by-list">
          {% assign logos = 'amalfilemon.webp,cafezal.webp,caffevergnano.webp,clinica botanica.webp,dream-factory.webp,enricorizzi.webp,guarnieri.webp' | split: ',' %}
          {% assign company_urls = 'https://www.clubjoy.it/search?q=amalfi+lemon+experience&options%5Bprefix%5D=last,https://www.clubjoy.it/search?q=cafezal&options%5Bprefix%5D=last,https://www.clubjoy.it/search?q=vergano&options%5Bprefix%5D=last,https://www.clubjoy.it/search?q=clinica+botanica&options%5Bprefix%5D=last,https://www.clubjoy.it/search?q=dream+factory&options%5Bprefix%5D=last,https://www.clubjoy.it/search?q=enrico+rizzi&options%5Bprefix%5D=last,https://www.clubjoy.it/search?q=guarnieri&options%5Bprefix%5D=last' | split: ',' %}

          {% for i in (0..6) %}
            <div class="logo-item">
              {% if company_urls[i] != blank %}
                <a href="{{ company_urls[i] }}" target="_blank" rel="noopener" class="logo-link">
                  <img
                    src="{{ logos[i] | asset_url }}"
                    alt="Company logo"
                    loading="lazy"
                    class="logo-image"
                  >
                </a>
              {% else %}
                <img
                  src="{{ logos[i] | asset_url }}"
                  alt="Company logo"
                  loading="lazy"
                  class="logo-image"
                >
              {% endif %}
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</div>
</div>
<script>
document.addEventListener("DOMContentLoaded", () => {
  const wrapper = document.querySelector(".js-chosen-by-list-wrapper");
  const list = document.querySelector(".js-chosen-by-list");
  const container = document.querySelector(".js-chosen-by-scroller");

  if (!wrapper || !list || !container) return;

  const fullWidth = list.offsetWidth;

  const clone1 = list.cloneNode(true);
  const clone2 = list.cloneNode(true);
  wrapper.appendChild(clone1);
  wrapper.appendChild(clone2);

  wrapper.style.setProperty('--chosen-by-scroll-distance', `-${fullWidth}px`);

  // Manual control variables
  let isDragging = false;
  let startX = 0;
  let currentTranslateX = 0;
  let animationPosition = 0;
  let userInteractionTimeout;
  let isUserInteracting = false;

  // Get current animation position
  function getCurrentAnimationPosition() {
    const computedStyle = getComputedStyle(wrapper);
    const matrix = computedStyle.transform;
    if (matrix === 'none') return 0;
    const values = matrix.split('(')[1].split(')')[0].split(',');
    return parseFloat(values[4]) || 0;
  }

  // Handle manual control start
  function startManualControl(clientX) {
    isDragging = true;
    isUserInteracting = true;
    startX = clientX;

    // Get current position from animation or previous manual position
    animationPosition = getCurrentAnimationPosition();
    currentTranslateX = animationPosition;

    wrapper.classList.add('user-interacting');
    container.classList.add('dragging');

    // Clear any existing timeout
    clearTimeout(userInteractionTimeout);
  }

  // Handle manual control move
  function handleManualMove(clientX) {
    if (!isDragging) return;

    const deltaX = clientX - startX;
    currentTranslateX = animationPosition + deltaX;

    // Apply immediate transform
    wrapper.style.transform = `translateX(${currentTranslateX}px)`;
  }

  // Handle manual control end
  function endManualControl() {
    if (!isDragging) return;

    isDragging = false;
    container.classList.remove('dragging');

    // Set timeout to resume auto-scroll after user stops interacting
    userInteractionTimeout = setTimeout(() => {
      isUserInteracting = false;
      wrapper.classList.remove('user-interacting');

      // Resume animation from current position
      const currentPos = getCurrentAnimationPosition();
      const remainingDistance = fullWidth + currentPos;
      const totalDistance = fullWidth;
      const remainingRatio = remainingDistance / totalDistance;

      if (remainingRatio > 0) {
        wrapper.style.animation = `chosen-by-scroll ${40 * remainingRatio}s linear infinite`;
        wrapper.style.animationDelay = '0s';
      }
    }, 2000); // Resume auto-scroll 2 seconds after user stops interacting
  }

  // Mouse events
  container.addEventListener('mousedown', (e) => {
    e.preventDefault();
    startManualControl(e.clientX);
  });

  document.addEventListener('mousemove', (e) => {
    handleManualMove(e.clientX);
  });

  document.addEventListener('mouseup', endManualControl);

  // Touch events for mobile
  container.addEventListener('touchstart', (e) => {
    e.preventDefault();
    const touch = e.touches[0];
    startManualControl(touch.clientX);
  }, { passive: false });

  container.addEventListener('touchmove', (e) => {
    e.preventDefault();
    const touch = e.touches[0];
    handleManualMove(touch.clientX);
  }, { passive: false });

  container.addEventListener('touchend', endManualControl);
  container.addEventListener('touchcancel', endManualControl);

  // Prevent text selection during drag
  container.addEventListener('selectstart', (e) => {
    if (isDragging) e.preventDefault();
  });

  // Handle hover pause/resume
  container.addEventListener('mouseenter', () => {
    if (!isUserInteracting) {
      wrapper.classList.add('manual-control');
    }
  });

  container.addEventListener('mouseleave', () => {
    if (!isUserInteracting) {
      wrapper.classList.remove('manual-control');
    }
  });
});
</script>

{% schema %}
{
  "name": "Chosen By Logos",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Scelto da"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle"
    },
    {
      "type": "select",
      "id": "logos_per_row",
      "label": "Logos per row",
      "options": [
        {
          "value": "2",
          "label": "2"
        },
        {
          "value": "3",
          "label": "3"
        },
        {
          "value": "4",
          "label": "4"
        },
        {
          "value": "5",
          "label": "5"
        },
        {
          "value": "6",
          "label": "6"
        }
      ],
      "default": "4"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "logo",
      "name": "Logo",
      "settings": [
        {
          "type": "image_picker",
          "id": "logo",
          "label": "Logo image"
        },
        {
          "type": "text",
          "id": "company_name",
          "label": "Company name"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link (optional)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Chosen By Logos",
      "blocks": [
        {
          "type": "logo"
        },
        {
          "type": "logo"
        },
        {
          "type": "logo"
        },
        {
          "type": "logo"
        }
      ]
    }
  ]
}
{% endschema %}